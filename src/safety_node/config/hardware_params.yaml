safety_node:
  ros__parameters:
    # Time-to-collision threshold in seconds.
    # RACE-PROVEN VALUE — tuned on hardware and used in competition alongside
    # wall_follow at up to 6.5 m/s. Do not raise casually: higher values cause
    # phantom braking at speed in narrow corridors.
    ttc_threshold: 0.35
